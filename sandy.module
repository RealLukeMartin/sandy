<?php
 
/**
 * Implements hook_menu().
 */
function sandy_menu() {
  // The paths given here need to match the ones in sandy.routing.yml exactly.
  $items['admin/sandy'] = array(
    'title' => 'Sandy',
    'description' => 'Sandy box demo',
    // The name of the route from sandy.routing.yml
    'route_name' => 'sandy',
  );
  return $items;
}